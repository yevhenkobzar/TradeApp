import { JournalEntry, PortfolioItem, Trade } from './types';

export const JOURNAL_ENTRIES: JournalEntry[] = [];

export const PORTFOLIO_ITEMS: PortfolioItem[] = [];

export const TRADES: Trade[] = [];